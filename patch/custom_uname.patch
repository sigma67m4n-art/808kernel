diff --git a/init/version.c b/init/version.c
index cba3411..2f12a57 100644
--- a/init/version.c
+++ b/init/version.c
@@ -45,11 +45,10 @@ EXPORT_SYMBOL_GPL(init_uts_ns);
 /* FIXED STRINGS! Don't touch! */
 const char linux_banner[] =
 	"Linux version " UTS_RELEASE " (" LINUX_COMPILE_BY "@"
-	LINUX_COMPILE_HOST ") (" LINUX_COMPILER ") " UTS_VERSION "\n";
+	LINUX_COMPILE_HOST ") " UTS_VERSION "\n";
 
 const char linux_proc_banner[] =
 	"%s version %s"
-	" (" LINUX_COMPILE_BY "@" LINUX_COMPILE_HOST ")"
-	" (" LINUX_COMPILER ") %s\n";
+	" (" LINUX_COMPILE_BY "@" LINUX_COMPILE_HOST ") %s\n";
 
 BUILD_SALT;
diff --git a/scripts/mkcompile_h b/scripts/mkcompile_h
index a72b154..642c8a0 100644
--- a/scripts/mkcompile_h
+++ b/scripts/mkcompile_h
@@ -29,16 +29,9 @@ if [ -z "$KBUILD_BUILD_TIMESTAMP" ]; then
 else
 	TIMESTAMP=$KBUILD_BUILD_TIMESTAMP
 fi
-if test -z "$KBUILD_BUILD_USER"; then
-	LINUX_COMPILE_BY=$(whoami | sed 's/\\/\\\\/')
-else
-	LINUX_COMPILE_BY=$KBUILD_BUILD_USER
-fi
-if test -z "$KBUILD_BUILD_HOST"; then
-	LINUX_COMPILE_HOST=`uname -n`
-else
-	LINUX_COMPILE_HOST=$KBUILD_BUILD_HOST
-fi
+
+LINUX_COMPILE_BY="tekqshi"
+LINUX_COMPILE_HOST="808kernel"
 
 UTS_VERSION="#$VERSION"
 CONFIG_FLAGS=""
